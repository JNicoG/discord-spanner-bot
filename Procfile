worker: java $JAVA_OPTS -jar target/*.jar

# java -Dserver.port=$PORT $JAVA_OPTS -jar target/*.jar
# The Procfile is used by Heroku to determine how to run the application.
# Convert back to web if needed e.g. reintroducing spring-boot-webmvc + actuator, adding prometheus,
# exposing api endpoints etc.